services:
  - type: web
    name: whatsapp-webhook
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn whatsapp_webhook:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: WHATSAPP_PHONE_NUMBER_ID
        value: 831503680056024
      - key: VERIFY_TOKEN
        value: abhishek_verify_123
